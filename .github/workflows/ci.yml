name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx prisma generate
      # Non-blocking for now: the repo has a backlog of pre-existing lint errors.
      # Remove `continue-on-error` to make lint required once that backlog is cleared.
      - run: npm run lint
        continue-on-error: true
      # Enable once the repo's pre-existing TypeScript errors are fixed:
      # - run: npx tsc --noEmit
      # Requires env (DATABASE_URL, NEXTAUTH_SECRET, JWT_SECRET, ...) via repo Actions secrets:
      # - run: npm run build
